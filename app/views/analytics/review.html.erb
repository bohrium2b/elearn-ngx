<% content_for :title, "Session Review" %>

<div class="container mx-auto px-4 py-8">
  <%= react_island_tag("session-review", {
    sessionId: @session.id,
    csrfToken: form_authenticity_token
  }) %>
</div>
