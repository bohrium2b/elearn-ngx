# frozen_string_literal: true

require "test_helper"

class TaxonomyNodesHelperTest < ActionView::TestCase
  test "should be defined" do
    assert(defined?(TaxonomyNodesHelper))
  end

  test "should be a module" do
    assert_kind_of Module, TaxonomyNodesHelper
  end
end
